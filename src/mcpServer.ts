/**
 * The Kraken i18n MCP server is now integrated directly into the VS Code extension.
 * It starts automatically when the extension activates and uses the extension's
 * configured settings (OpenAI API key, model, i18n folder, source language).
 *
 * See McpTab.tsx / the "MCP" tab inside the extension for connection details.
 */
export {};

